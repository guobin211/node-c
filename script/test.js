const { getAllProjects } = require('./config')
const { spawn } = require('child_process')

// const projects = getAllProjects();
// for (const name in projects) {
//   const task = spawn('node', [projects[name]])
// }

